import React from 'react';
import {logo} from './../theme/images'

const Header = () => {
  return (
    <header className="header">
        <img src={logo} />
    </header>
  )
}

export default Header;